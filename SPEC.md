# 栃木の直売マルシェ 仕様書

## 1. サービス概要

**目的**
知り合いベースで直売している小規模農家が、知り合い以外の新規顧客にも「顔の見える直売」を広げられる場をつくる。あわせて、余剰・規格外品の販売と、畑でのイベント告知・収穫期の人手募集を1つの農家ページに集約する。

**対象地域**
栃木県内限定(市町村単位で表示・絞り込み)

**対象ユーザー**
- 農家: 知り合いに直売しているが、新規顧客も増やしたい小規模農家
- 利用者: 栃木の農産物を買いたい人、地域のイベントに参加したい人、収穫を手伝いたい人

**基本方針**
- 決済機能は実装しない。購入・支払いは利用者と農家が直接やり取り(現金・PayPay送金など農家が決める方法)
- サービス内メッセージ機能は作らない。連絡はLINE公式アカウントまたは電話番号へのリンクのみ
- 農家がスマホから片手で更新できる手軽さを最優先(入力項目を極力減らす)

## 2. 情報構造

サービスは3種類の情報オブジェクトで構成する。

| オブジェクト | 内容 | 保有する主な項目 |
|---|---|---|
| 農家(プロフィール) | 農家の基本情報 | 農家名、所在地(市町村)、作っているもの、こだわり・紹介文、写真、連絡先(LINE URL or 電話番号) |
| 出品 | 今買えるもの | 写真、品目名、一言コメント、価格(任意)、通常出品/わけあり・特価の区分、投稿日時 |
| イベント | 畑で開催すること | イベント名、日時、場所、内容の一言説明、参加費(任意) |

出品・イベントはいずれも農家プロフィールに紐づく(1農家が複数の出品・イベントを持てる)。

## 3. 画面仕様

### 3-1. 農家一覧画面(トップ)
- 栃木県内の登録農家をカード形式で一覧表示
- 市町村タブで絞り込み可能(宇都宮市、足利市、那須塩原市 等)
- 各カードに表示する情報: 農家名、所在地、主な作物、サムネイル画像
- 状態バッジ:「本日出品あり」(直近出品がある場合)、「〇/〇 イベント」(直近開催予定がある場合)
- 並び順: 出品・イベントの更新が新しい農家を上位表示(検索機能は初期版では設けない)
- 画面上部に「リスト/マップ」切り替えを設置し、3-6のマップ画面へ切り替えられるようにする

### 3-2. 農家ページ
- ヘッダー: カバー写真、農家名、所在地、作っているもの、紹介文
- 連絡ボタン: タップでLINE公式または電話発信に直接遷移(サービス内には連絡フォームを作らない)
- 出品リスト: 写真+品目名+一言コメント+価格(任意)。わけあり・特価品は別バッジで区別
- イベントリスト: 日付+イベント名+一言説明+時間・参加費
- 人手募集: イベントと同じ投稿UIを流用し、「〇月〇日 収穫手伝い募集」のように登録(初期版ではイベントの一種として扱う)

### 3-3. 出品一覧画面(横断・MVP後に追加)
- 全農家の出品を新着順に一覧表示
- 農家を知らない新規ユーザー向けの入口
- タップで該当の農家ページへ遷移

### 3-4. イベント一覧画面(横断・MVP後に追加)
- 全農家のイベントをカレンダーまたは日付順一覧で表示
- 農業に直接関心がない層(家族連れ、地域住民)向けの入口

### 3-5. 農家向け投稿画面
- 出品投稿: 写真1枚+品目名+一言コメント(価格は任意)。入力から投稿完了まで30秒以内を目標とする
- イベント投稿: イベント名+日時+場所+一言説明
- 過去の投稿を編集・削除できる最低限の管理機能

### 3-6. マップ画面
- 栃木県内の登録農家を地図上にピン表示する
- 利用者の現在地を取得し(位置情報の利用許可が前提)、現在地から近い順に農家を並べたリストを地図の下に表示する
- 位置情報の利用を許可しない場合でも、市町村名などから地図の表示エリアを選べるようにする
- ピンをタップすると農家名・主な作物・状態バッジ(出品/イベントあり)を簡易表示し、そこから農家ページへ遷移する
- 出品一覧・イベント一覧(3-3、3-4)からも、それぞれの地点を同じ地図上に表示できるようにする(将来拡張)

## 4. 機能一覧

| 機能 | 優先度 | 備考 |
|---|---|---|
| 農家一覧表示(市町村絞り込み) | MVP | |
| 農家ページ(出品+イベント表示) | MVP | |
| 農家による出品投稿 | MVP | 写真+品目名+一言が最低限 |
| 農家によるイベント投稿 | MVP | 人手募集もこの機能で兼用 |
| LINE/電話への連絡導線 | MVP | サービス内メッセージ機能なし |
| マップ表示(農家の位置をピン表示) | MVP後 | 地図APIの導入が必要 |
| 現在地から近い農家順の表示 | MVP後 | 位置情報の利用許可が前提。許可なしの場合は市町村選択で代替 |
| 出品一覧(横断) | MVP後 | 農家3〜5軒集まってから追加 |
| イベント一覧(横断) | MVP後 | 同上 |
| 決済機能 | 対象外 | 実装しない方針 |
| サービス内メッセージ機能 | 対象外 | LINE等に委ねる方針 |

## 5. 非機能要件・運用方針
- 決済なし: 金銭のやり取りは農家と利用者の直接対応。トラブル時もサービスは仲介しない旨を利用規約に明記する
- 更新のしやすさ: 農家側の継続利用がサービスの生命線のため、投稿UIは常にスマホ入力を前提に最小項目で設計する
- エリア限定の訴求:「栃木の農家限定」「顔が見える・地元だから安心」を前面に出したデザイン・コピーにする
- 営業時の見せ方: 出品・イベントの投稿・閲覧はすべて無料である旨を明確にし、農家が導入コストを感じない説明にする
- 位置情報の扱い: 現在地取得は利用者の任意とし、許可しない場合も市町村選択でマップ・一覧を利用できるようにする(位置情報がないと使えない設計にしない)

## 6. MVP範囲(最初のリリース)
- 農家一覧画面
- 農家ページ(出品+イベント表示、LINE/電話連絡導線)
- 農家向け投稿画面(出品・イベント)

出品一覧・イベント一覧(横断)は、登録農家が3〜5軒集まった段階で追加する。マップ表示・現在地からの近い順検索も、地図APIの導入コストがかかるため同じタイミング以降での追加を想定する。

## 7. 今後の検討事項
- 出品・イベント投稿をLINE Bot経由でできるようにする(チャット感覚での投稿)
- 農家ページのアクセス数など簡易な効果測定の仕組み
- 対象エリアの拡大(栃木県内の他市町村、将来的な県外展開)
